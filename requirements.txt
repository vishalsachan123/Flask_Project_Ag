# Core requirements
flask>=2.0.0
flask-socketio>=5.3.0
python-dotenv>=1.0.0
eventlet>=0.35.0  # Required for async_mode='eventlet'

# AutoGen requirements (as per official docs)
autogen-agentchat>=0.2.0
autogen-ext[openai,azure]>=0.2.0

# Azure/OpenAI dependencies
openai>=1.0.0
azure-identity>=1.12.0
azure-search-documents>=11.4.0

# Production essentials
gunicorn>=20.1.0  # WSGI server for Render
greenlet>=3.0.0  # Required by eventlet
pyopenssl>=23.2.0  # SSL support
cryptography>=40.0.0  # Security

# Logging/monitoring
blinker>=1.7.0  # Flask signals
sentry-sdk>=1.40.0  # Error tracking (optional)

# Your existing dependencies
aiofiles>=23.2.0
pydantic>=2.0.0
tiktoken>=0.5.0
requests>=2.28.0
python-dotenv>=1.0.0
